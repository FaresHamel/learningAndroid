### learningAndroid

### git clone https://name or link respository

### git add nameFille for add to gitStag
### git reset head nameFolder or namFille for diistroy to stage

### git branch
### git remote -v

### git config --global user.email"your email@gmail.com" user.name"deine userNameGitHub"

 #### for localRespository 
  
  1. git clone "link"
  2. git status
  3. git add nameFolder or nameFille
  4. git commit -m "Desciption"
  5. git status 
  
 #### for respository web (git hub)push 
 
   1.  git branch for git your real branch
   2.  git remote -v for git remote respository
   3. git remoteName branchName

### Deleted Directory

 1. lsof nameDirectory for empty
 2. rm -r or rm -rf or rmdir nameDirectoryEmpty.  
